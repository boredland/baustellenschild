{
  "name": "baustellenschild",
  "private": true,
  "description": "Repo-root entry point so Cloudflare Workers Builds works from either root directory",
  "scripts": {
    "build": "npm --prefix web ci && npm --prefix web run build",
    "deploy": "npm run build && npx wrangler deploy --config web/wrangler.jsonc"
  }
}
